---
import "../styles/bibGlobal.css";
---

<header>
  <div class="headerBar">
    <div class="bookmarksButtonContainer">
      <a class="bookmarksIcon"></a>
      <a class="bookmarksText">Bookmarks</a>
    </div>
    <a href="/" class="logo"></a>
    <div class="accountButtonContainer">
      <a href="/Account" class="accountIcon"></a>
      <a href="/Account" class="accountText">My Account</a>
    </div>
  </div>
</header>

<style>
  .headerBar {
    height: var(--HeaderHeight);
    font-family: Modern Sans, Arial, sans-serif;
    background: linear-gradient(to bottom, var(--BibBlue), var(--BibBlueVar));
    display: grid;
    grid-template-columns: repeat(3, 1fr);
  }

  .logo {
    grid-column: 2;
    justify-self: center;
    background-image: url("/logos/biblioplexLogo.svg");
    height: 60%;
    aspect-ratio: 1/1;
    margin-block: auto;
    cursor: pointer;
  }
  .accountButtonContainer {
    grid-column: 3;
    display: flex;
    flex-direction: row-reverse;
    height: calc(0.3 * var(--HeaderHeight));
    margin-inline: var(--SmallGap);
    margin-block: auto;
  }

  .accountIcon {
    align-self: center;
    height: 100%;
    aspect-ratio: 1/1;
    background-image: url("/logos/accountIcon.svg");
    background-size: cover;
    margin-left: 1rem;
    transition: background-image 0.25s;
  }

  .accountButtonContainer:hover > .accountIcon {
    background-image: url("/logos/accountIconHovered.svg");
  }
  .accountText {
    color: white;
    align-self: center;
    text-align: right;
    font-size: 1.3rem;
    font-weight: 700;
    white-space: nowrap;
    transition: color 0.25s;
  }

  .accountButtonContainer:hover > .accountText {
    color: var(--HoveredMenu);
  }

  .bookmarksButtonContainer {
    grid-column: 1;
    display: flex;
    height: calc(0.3 * var(--HeaderHeight));
    margin-inline: var(--SmallGap);
    margin-block: auto;
  }

  .bookmarksIcon {
    align-self: center;
    height: 100%;
    aspect-ratio: 1/1;
    background-image: url("/logos/bookmarksIcon.svg");
    background-size: cover;
    margin-right: 1rem;
    transition: background-image 0.25s;
  }

  .bookmarksButtonContainer:hover > .bookmarksIcon {
    background-image: url("/logos/bookmarksIconHovered.svg");
    cursor: pointer;
  }
  .bookmarksText {
    color: white;
    align-self: center;
    font-size: 1.3rem;
    font-weight: 700;
    white-space: nowrap;
    transition: color 0.25s;
  }

  .bookmarksButtonContainer:hover > .bookmarksText {
    color: var(--HoveredMenu);
    cursor: pointer;
  }

  @media (width < 500px) {
    .bookmarksText,
    .accountText {
      display: none;
    }
  }
</style>
